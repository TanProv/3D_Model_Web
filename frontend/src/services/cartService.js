export const cartService = {
    // Có thể thêm các hàm xử lý API liên quan đến giỏ hàng
    saveCartToServer: async (cartData, userId) => {
        // Gọi API lưu giỏ hàng lên server
    },
    
    loadCartFromServer: async (userId) => {
        // Gọi API lấy giỏ hàng từ server
    }
};